$('.answer-errors').html("<%= render 'shared/errors', resource: @answer %>");
<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>');
  $('#answer-body-create').val('')
  $('#answer-files-create').val('')
  $("#Link\\ name").val('')
  $("#Url\\ path").val('')
<% end %>
