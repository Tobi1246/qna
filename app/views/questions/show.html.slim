h1= @question.title 

= button_to 'DeleteQ', question_path(@question), method: :delete if current_user&.author?(@question)
h2= "body"
p= @question.body
h3= "answers"
p= render @question.answers
= render "answers/form" if user_signed_in? 

= link_to "All questions", questions_path
